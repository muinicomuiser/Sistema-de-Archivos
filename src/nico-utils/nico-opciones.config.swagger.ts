/**Selección de información que se querrá incorporar en la configuración de Swagger. */
type OpcionesConfig = {
  title?: boolean;
  description?: boolean;
  version?: boolean;
  authorName?: boolean;
  authorUrl?: boolean;
  authoEmail?: boolean;
  license?: boolean;
  ambiente?: boolean;
};
